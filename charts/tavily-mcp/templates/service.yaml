apiVersion: v1
kind: Service
metadata:
  name: {{ include "tavily-mcp.fullname" . }}
  labels:
    {{- include "tavily-mcp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  sessionAffinity: ClientIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tavily-mcp.selectorLabels" . | nindent 4 }}
