Tavily MCP server deployed successfully.

Internal endpoint (streamable-http):
  http://{{ include "tavily-mcp.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}/mcp

Legacy SSE endpoint:
  http://{{ include "tavily-mcp.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}/sse

To test:
  kubectl run curl-test --rm -it --image=curlimages/curl -- \
    curl -v http://{{ include "tavily-mcp.fullname" . }}.{{ .Release.Namespace }}.svc.cluster.local:{{ .Values.service.port }}/status
