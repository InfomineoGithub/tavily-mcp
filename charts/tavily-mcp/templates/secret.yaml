{{- if not .Values.secret.existingSecret }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "tavily-mcp.fullname" . }}
  labels:
    {{- include "tavily-mcp.labels" . | nindent 4 }}
type: Opaque
data:
  TAVILY_API_KEY: {{ .Values.secret.tavilyApiKey | b64enc | quote }}
{{- end }}
